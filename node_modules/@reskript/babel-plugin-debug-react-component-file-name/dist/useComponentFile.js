import { useDebugValue, useRef } from 'react';
export default function useComponentFile(displayValue, fullValue) {
    useDebugValue(displayValue);
    useRef(fullValue.replace(/:origin/g, location.origin));
}
