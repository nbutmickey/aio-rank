'use strict';

var willBreakingInternalUseOnly = require('@reskript/core/willBreakingInternalUseOnly');
var lessSyntax = require('postcss-less');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var lessSyntax__default = /*#__PURE__*/_interopDefaultLegacy(lessSyntax);

const config = {
  extends: willBreakingInternalUseOnly.resolveSync('@ecomfe/stylelint-config'),
  overrides: [{
    files: ['**/*.less'],
    customSyntax: lessSyntax__default["default"]
  }]
};

module.exports = config;
