import { ProjectSettings, ClientProjectSettings, CommandInput } from './interface/index.js';
type Plugins = ClientProjectSettings['plugins'];
export declare const applyPlugins: (settings: ProjectSettings, plugins: Plugins, cmd: CommandInput) => Promise<ProjectSettings>;
export {};
